{
  "scripts": {
    "instalar:todo": "npm install --prefix backend && npm install --prefix frontend",
    "instalar:ia": "node scripts/instalar-ia.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:stop": "node scripts/detener-dev.mjs",
    "dev:ia": "node scripts/servicio-ia.mjs",
    "seed:carga-masiva": "node backend/scripts/seed-reportes-masivos.mjs",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test",
    "architecture:check": "node scripts/architecture-check.mjs backend && node scripts/architecture-check.mjs frontend && node scripts/architecture-check.mjs ai-service"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
